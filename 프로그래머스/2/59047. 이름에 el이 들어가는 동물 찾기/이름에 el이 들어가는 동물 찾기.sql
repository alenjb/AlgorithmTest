SELECT ANIMAL_ID, NAME
FROM ANIMAL_INS
WHERE UPPER(NAME) LIKE "%EL%" AND ANIMAL_TYPE = 'Dog'
ORDER BY NAME














# SELECT ANIMAL_ID,NAME
# FROM ANIMAL_INS
# WHERE LOWER(NAME) LIKE LOWER('%el%')
# AND ANIMAL_TYPE = 'Dog'
# ORDER BY NAME